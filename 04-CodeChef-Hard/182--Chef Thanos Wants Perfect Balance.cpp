#include<bits/stdc++.h>

using namespace std;

int main(){
	
	ios_base :: sync_with_stdio(0);
	cin.tie(0); cout.tie(0);
	
	int t, n, m, i, a, b;
	
	cin >> t;
	
	while(t --){
		cin >> n >> m;
		
		for(i = 1; i <= m; i ++){
			cin >> a >> b;
		}
		
		for(i = 1; i <= m; i ++) cout << 0 << endl; 
	}
	
	return 0;
}
